# ========================================
# React 项目环境变量配置示例
# ========================================
#
# 使用说明：
# 1. 复制此文件：cp .env.example .env.dev（开发环境）
# 2. 复制此文件：cp .env.example .env.test（测试环境）
# 3. 复制此文件：cp .env.example .env.prod（生产环境）
# 4. 根据环境修改相应的配置
# 5. 开发环境：docker-compose --env-file .env.dev --profile dev up -d
#    生产环境：docker-compose --env-file .env.prod --profile prod up -d

# ========================================
# 应用配置
# ========================================

# 运行环境：development/testing/production
ENV=development

# Node 环境
NODE_ENV=development

# 镜像名称和标签
IMAGE_NAME=myapp
IMAGE_TAG=latest

# 容器名称
CONTAINER_NAME=myapp-dev

# ========================================
# 端口配置（重要！）
# ========================================

# 应用端口（容器内部端口）
# 开发环境默认：5173（Vite）
# 生产环境默认：80（Nginx）
APP_PORT=5173

# 宿主机端口（映射到外部的端口）
# 开发环境示例：5173
# 测试环境示例：5174 或 3001
# 生产环境示例：80 或 443
HOST_PORT=5173

# ========================================
# npm 镜像源配置
# ========================================

# npm 源（淘宝源，推荐）
NPM_REGISTRY=https://registry.npmmirror.com

# 可选源：
# - https://registry.npmmirror.com（淘宝）
# - https://mirrors.cloud.tencent.com/npm/（腾讯云）
# - https://repo.huaweicloud.com/repository/npm/（华为云）

# ========================================
# Vite 环境变量
# ========================================
# 注意：Vite 只会暴露以 VITE_ 开头的环境变量到客户端代码

# API 基础 URL
# 开发环境：http://localhost:8000
# 测试环境：https://test-api.example.com
# 生产环境：https://api.example.com
VITE_API_URL=http://localhost:8000

# 应用标题
VITE_APP_TITLE=My App

# 应用环境（会在客户端代码中使用）
# 注意：这是 Vite 环境变量，不同于 NODE_ENV
VITE_ENV=development

# 其他自定义环境变量（以 VITE_ 开头）
# VITE_ANALYTICS_ID=
# VITE_SENTRY_DSN=
# VITE_FEATURE_FLAG_X=true

# ========================================
# 后端 API 配置（可选）
# ========================================
# 如果 docker-compose 中包含后端服务

# BACKEND_CONTAINER_NAME=myapp-backend
# BACKEND_PORT=8000
# DATABASE_URL=postgresql://user:password@db:5432/myapp

# ========================================
# 多环境配置示例
# ========================================

# --- 开发环境 (.env.dev) ---
# ENV=development
# NODE_ENV=development
# HOST_PORT=5173
# APP_PORT=5173
# VITE_API_URL=http://localhost:8000
# VITE_ENV=development

# --- 测试环境 (.env.test) ---
# ENV=testing
# NODE_ENV=production
# HOST_PORT=5174
# APP_PORT=80
# VITE_API_URL=https://test-api.example.com
# VITE_ENV=testing

# --- 生产环境 (.env.prod) ---
# ENV=production
# NODE_ENV=production
# HOST_PORT=80
# APP_PORT=80
# VITE_API_URL=https://api.example.com
# VITE_ENV=production
# VITE_ANALYTICS_ID=<your-analytics-id>
# VITE_SENTRY_DSN=<your-sentry-dsn>
